version: 0.0
os: linux

files:
  - source: /
    destination: /usr/share/nginx/html

permissions:
  - object: /usr/share/nginx/html
    owner: nginx
    group: nginx
    mode: 755

hooks:
  AfterInstall:
    - location: scripts/install.sh
      timeout: 300
      runas: root
